// Subarray Sums I
// CSES Sorting and Searching: https://cses.fi/problemset/task/1660

#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, x;
    cin >> n >> x;

    int arr[n];
    for (int& num: arr) cin >> num;

    int left = 0, right = 0;
    int sum = 0;

    while (sum != x) {
        
    }
}